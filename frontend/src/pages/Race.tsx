import {Card} from "../components/Card";
import {DefaultLayout} from "../layouts/DefaultLayout";

export const Race = () => {
    return (
        <DefaultLayout>
            <Card>

            </Card>
        </DefaultLayout>
    );
}